extends layout

block content
    h1.mark Delete #{collection} ?

    form(method='POST',action='/delete/'+collection)
        br
        li Secret:
        input.form-control(name='secret', type='password')
        br
        input.btn.btn-danger(type='submit',value='Confirm deletion')

    br
    a.btn.bg-info.text-white(href="/show/"+collection) Back
    br
    br